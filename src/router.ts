import { Routes } from '@angular/router';

export const routerConfig: Routes = [
    {
        path: '',
        /*children: [{
            path: '',
            component: MainMenuComponent
        }]*/
    }
];